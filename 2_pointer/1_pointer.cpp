#include<stdio.h>
int main(){
    int *pc,c;
    c=5;
    pc=&c;
    printf("value of adress of c is given by pointer variable %;p,pc");
    printf("value of value of c is given by *pc i.e %d", *pc);

}